[build]
  base = "."
  command = "pnpm --filter @workspace/prymedge run build"
  publish = "artifacts/prymedge/dist/public"

[build.environment]
  NODE_VERSION = "20"
  BASE_PATH = "/"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
