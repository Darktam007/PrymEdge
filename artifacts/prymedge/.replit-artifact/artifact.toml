kind = "web"
previewPath = "/"
title = "PrymEdge"
version = "1.0.0"
id = "artifacts/prymedge"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 18853

[services.development]
run = "pnpm --filter @workspace/prymedge run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/prymedge", "run", "build" ]
publicDir = "artifacts/prymedge/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "18853"
BASE_PATH = "/"
